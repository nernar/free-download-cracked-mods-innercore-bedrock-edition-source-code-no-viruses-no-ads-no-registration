/*
Заморожено


Structure.init("structures");

Callback.addCallback("GenerateChunk", function(chunkX, chunkZ, random, dimensionId){ 

// Var coords

   var coords = GenerationUtils.randomCoords(chunkX, chunkZ, 0, 128);

var co = GenerationUtils.randomCoords(chunkX, chunkZ, 0, 128);

 var coo = GenerationUtils.randomCoords(chunkX, chunkZ, 0, 128);
    
var coord = GenerationUtils.randomCoords(chunkX, chunkZ, 0, 128);

var coor = GenerationUtils.randomCoords(chunkX, chunkZ, 0, 128);


// Structure setInWorld
if(World.getBlockID(coord.x,coord.y,coord.z) == 2){ 
    World.setBlock(coord.x,coord.y,coords.z, 5); 
    Structure.setInWorld("medium", coord.x, coord.y+1, coord.z); 
   }

if(World.getBlockID(coo.x,coo.y,coo.z) == 2){ 
    World.setBlock(coo.x,coo.y,coo.z, 5); 
    Structure.setInWorld("fall", coo.x, coo.y+1, coo.z); 
   }

if(World.getBlockID(coord.x,coord.y,coord.z) == 2){ 
    World.setBlock(coord.x,coord.y,coord.z, 5); 
    Structure.setInWorld("low", coord.x, coord.y+1, coord.z); 
   } 
 if(World.getBlockID(coords.x,coords.y,coords.z) == 2){ 
    World.setBlock(coords.x,coords.y,coords.z, 5); 
    Structure.setInWorld("koster", coords.x, coords.y+2, coords.z); 
   } 
  });
*/






